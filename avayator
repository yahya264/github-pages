import React from "react";
import { BrowserRouter as Router, Route, Routes, Link } from "react-router-dom";

const products = [
  {
    id: 1,
    name: "Lunettes Ray-Ban",
    price: 149.99,
    image: "https://via.placeholder.com/300x200.png?text=Ray-Ban",
  },
  {
    id: 2,
    name: "Lunettes Gucci",
    price: 219.99,
    image: "https://via.placeholder.com/300x200.png?text=Gucci",
  },
  {
    id: 3,
    name: "Lunettes Dior",
    price: 199.99,
    image: "https://via.placeholder.com/300x200.png?text=Dior",
  },
];

const Navbar = () => (
  <nav className="bg-black text-white p-4 flex justify-between">
    <h1 className="text-xl font-bold">Avyator Optic</h1>
    <div className="space-x-4">
      <Link to="/">Accueil</Link>
      <Link to="/products">Produits</Link>
      <Link to="/contact">Contact</Link>
    </div>
  </nav>
);

const Home = () => (
  <div className="p-6 text-center">
    <h2 className="text-2xl font-semibold mb-4">Bienvenue chez Avyator Optic</h2>
    <p>Des lunettes de marque pour un style inégalé.</p>
  </div>
);

const Products = () => (
  <div className="p-6 grid grid-cols-1 md:grid-cols-3 gap-6">
    {products.map((product) => (
      <div key={product.id} className="border rounded-2xl shadow p-4">
        <img src={product.image} alt={product.name} className="w-full h-40 object-cover rounded" />
        <h3 className="text-lg font-semibold mt-2">{product.name}</h3>
        <p className="text-gray-600">{product.price} €</p>
        <button className="mt-2 bg-black text-white px-4 py-2 rounded-xl">Ajouter au panier</button>
      </div>
    ))}
  </div>
);

const Contact = () => (
  <div className="p-6">
    <h2 className="text-xl font-bold mb-2">Contactez-nous</h2>
    <p>Email : contact@avyatoroptic.com</p>
    <p>Téléphone : +212 6 00 00 00 00</p>
  </div>
);

const App = () => (
  <Router>
    <div className="font-sans">
      <Navbar />
      <Routes>
        <Route path="/" element={<Home />} />
        <Route path="/products" element={<Products />} />
        <Route path="/contact" element={<Contact />} />
      </Routes>
    </div>
  </Router>
);

export default App;
